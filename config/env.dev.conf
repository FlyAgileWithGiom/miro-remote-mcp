# Development environment configuration
# Loaded by scripts/deploy.sh when ENV=dev

# Server connection
SERVER_HOST="dev.notes.duquesnay.fr"
SERVER_USER="root"
DEPLOY_PATH="/opt/miro-mcp"

# Package name
TARBALL_NAME="miro-mcp-dev-deploy.tar.gz"

# Docker command
DOCKER_COMPOSE="docker compose"

# Environment file
ENV_FILE=".env.dev"
