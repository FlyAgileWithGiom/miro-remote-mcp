# Production environment configuration
# Loaded by scripts/deploy.sh when ENV=production

# Server connection
SERVER_HOST="notes.duquesnay.fr"
SERVER_USER="root"
DEPLOY_PATH="/opt/miro-mcp"

# Package name
TARBALL_NAME="miro-mcp-deploy.tar.gz"

# Docker command
DOCKER_COMPOSE="docker compose"

# Environment file
ENV_FILE=".env.production"
